{{- if .Values.global.dashboards.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: coder-dashboard-prebuilds
  namespace: {{ .Release.Namespace }}
  {{- with .Values.global.dashboards.configmapLabels }}
  labels: {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  coder-prebuilds.json: |- {{- include "prebuilds-dashboard.json" . | trim | nindent 4 }}
{{- end }}
