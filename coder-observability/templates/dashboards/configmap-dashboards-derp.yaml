{{- if .Values.global.dashboards.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: coder-dashboard-derp
  namespace: {{ .Release.Namespace }}
  {{- with .Values.global.dashboards.labels }}
  labels: {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  coder-derp.json: |- {{- include "derp-dashboard.json" . | trim | nindent 4 }}
{{- end }}
