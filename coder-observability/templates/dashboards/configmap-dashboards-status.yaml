{{- if .Values.global.dashboards.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: coder-dashboard-status
  namespace: {{ .Release.Namespace }}
  {{- with .Values.global.dashboards.configmapLabels }}
  labels: {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  coder-status.json: |- {{- include "status-dashboard.json" . | trim | nindent 4 }}
{{- end }}
