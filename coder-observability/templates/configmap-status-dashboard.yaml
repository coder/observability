apiVersion: v1
kind: ConfigMap
metadata:
  name: status-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  status-dashboard.json: |- {{- include "status-dashboard.json" . | trim | nindent 4 }}